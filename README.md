# tac_infra
A private infra for VTLA

# git useage
    # 开始写代码前
    git pull --rebase origin main

    # 写完后
    git add .
    git commit -m "一个说明"
    git push origin main

    # 另一台服务器同步
    git pull origin main