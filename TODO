动作和state兼容joint / episode_ee + relative_ee（与 umi 同步）——训练侧已完成（act/diffusion/pi05/starvla_groot），
离线脚本 vtla/datasets/convert_joints_to_eepose.py 给数据集加 observation.state_episode_ee /
action_episode_ee / action_relative_ee(stats)。验证：tests/test_ee_transforms.py、test_ee_datapath.py、
test_ee_deploy_roundtrip.py。

[待做] EE 部署硬件 plumbing（需接上机器人联调，无法离线验证；会给真臂下发位姿，安全相关）：
  1. robot get_observation(ee)：读关节→FK→A_t→S_t=A0^-1·A_t，episode 开始缓存 A0，按 build_names()
     发出 20 维 EE state（right 在前，每臂 [pos,rot6d,grip]）。
  2. robot send_action(ee)：收 postproc 出的 S_{t+k}→A_{t+k}=ee_to_absolute(A0,S)→rm_movep_canfd(pose)
     直接执行（位姿无需 IK），或 Algo IK→send_joints。
  3. record-engine：episode 开始重置 A0；observation_features/action_features 按 mode 切换。
  数学已离线证明（test_ee_deploy_roundtrip.py：pose 闭环 1e-16、FK(IK(pose))==pose）。

rewrite collect.sh
manner to save traing log